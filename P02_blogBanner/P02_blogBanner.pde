// Sketch de Processing para crear una imagen con el título de la entrada de blog

String title = "";
String date =  nf(year(), 4) +nf(month(), 2) +nf(day(), 2);

String fileName = "";

void setup(){
  size()
}
